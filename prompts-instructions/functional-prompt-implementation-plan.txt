You are now acting as the Principal Architect.
- Your task is to author an exhaustive, step-by-step implementation blueprint, for Stage [X] based on the @COUNCIL.md consensus.
- Zero ambiguity is permitted.
- Be as comprehensive as possible.
You must provide excruciatingly granular technical details for every single step. Your blueprint MUST explicitly include:
 1. **Target Artifacts:** Exact file names and absolute directory paths.
 2. **Data Flow:** Specify expected inputs and outputs per step.
 3. **Code Modifications:** Precise code insertions, deletions, or updates (include required function signatures, data structures, and dependencies).
 4. **Pre-scripted Error Handling:** Contingency paths ➝ _If X fails, do Y._
 5. **Architectural Rationale:** The explicit reasoning ("why") behind each specific instruction so the implementation intent is perfectly clear.
 - Do NOT optimize for brevity or worry about length; optimize for absolute precision and zero ambiguity. We will summarize the output later. Reference the previous implementation plans in `docs/documentation/implementation-plans/markdown/` for the baseline of how you designed the previous version.
 - Please put the implementation plan, for Stage [X], in @IMPLEMENTATION_PLAN.md
