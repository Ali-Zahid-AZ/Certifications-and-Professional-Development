You are a design auditor for /home/az/GitHub-Repositories/local-mi-article. Read ALL 7 documents in `docs/primitives/markdown/` and extract what the project's OWN DESIGN DOCS say the investigations SHOULD be doing — so we can judge whether the actual experiments (the 19-experiment suite) match the intended design.

Files (all <82KB, safe for rtk read, but check stat first):
- docs/primitives/markdown/BLUEPRINT.md (17KB)
- docs/primitives/markdown/DETAILED-EXPLANATION-BLUEPRINT.md (14KB)
- docs/primitives/markdown/initiation-01-COUNCIL.md (45KB)
- docs/primitives/markdown/initiation-02-COUNCIL.md (81KB — use native Read offset/limit if rtk truncates)
- docs/primitives/markdown/literature-for-the-project.md (27KB)
- docs/primitives/markdown/llm-models-used-in-this-study.md (20KB)
- docs/primitives/markdown/rough-sketch-article-skeleton.md (8KB)

Use `rtk read <file>` (check `stat -c %s` first; if >32KB use native Read). Do NOT spawn sub-agents (depth-1). Do NOT modify files.

Extract and report:

1. **Article skeleton:** From rough-sketch-article-skeleton.md, what is the planned structure of the paper "MI on 8 GB: A Fidelity-Frontier Study Across Geometry, Circuits, and Features"? What are the load-bearing claims/sections? What experiments does the skeleton expect to support each section?

2. **The 6 literature papers:** From literature-for-the-project.md, what are the 6 papers, and what role does each play in the project (reproduce vs extend vs supporting)? Map each to the project's experiment tracks (geometry helix / IOI circuit / ACDC / SAE features / SFC / tuned lens).

3. **Models used:** From llm-models-used-in-this-study.md, which models are used for which experiment (GPT-J-6B for helix, GPT-2-small for IOI, Pythia-160m for SAE/features, etc.)? Any model↔experiment mapping that matters for faithfulness?

4. **Blueprint:** From BLUEPRINT.md + DETAILED-EXPLANATION-BLUEPRINT.md, what is the intended experimental design? What is the fidelity-frontier concept? What is the 5-tier fidelity protocol (Tier 1 metric gate / Tier 2 residual cosine / Tier 2b feature Jaccard / Tier 2c semantic rank-corr / Tier 3 circuit faithfulness)? What are the cross-technique comparisons the paper promises?

5. **Initiation COUNCIL context:** From initiation-01-COUNCIL.md + initiation-02-COUNCIL.md, what were the ORIGINAL architectural decisions? Was fp16-CPU always the ground truth? Was RTN always the quantizer? Was bnb always quarantined? What was the 8GB envelope strategy? Any decision that the current experiments might be violating?

6. **Design-vs-implementation gaps:** Cross-reference the design docs against the 19-experiment suite (you don't need to read the experiment code — just the design docs). Are there experiments the design promised that are MISSING or incomplete? Are there experiments running that the design did NOT call for? Is the "fidelity frontier" (the precision at which each mechanism class breaks) actually being produced for all 3 mechanism classes (geometry / circuits / features)?

7. **The fp16-vs-RTN faithfulness question:** Do the design docs explicitly state that fp16 is the baseline and RTN is the faithful quant arm? Do they define what "faithful" means operationally? Do they anticipate the issues the 18:27 audit found (seed-inertness, fp32-vs-fp16, per-precision subset)?

RETURN: 7 sections. Then a 4-sentence verdict: (a) Is the experiment suite faithful to the project's OWN design intent? (b) What is the single biggest design-vs-implementation gap? (c) Is the science (fp16 ground truth → RTN sweep → mechanism preservation) the right design for the research question? (d) What is missing for the paper to be submittable?

Be rigorous.

---

You are an independent auditor verifying claims in a mechanistic-interpretability research project at /home/az/GitHub-Repositories/local-mi-article. Your job is to independently verify, by reading the ACTUAL code and disk artifacts, a set of claims made by a prior agent (Opus) in AGENT_CHANGES.md at 2026-07-04 18:27:09 PKT about fp16-baseline vs RTN faithfulness for the CIRCUITS track. DO NOT trust the prose claims — verify each one against code + disk and report CONFIRMED / REFUTED / PARTIAL / CANNOT-VERIFY with exact file:line evidence.

Use these tools: `rtk read <file>` for whole files (but check `stat -c %s <file>` first — if >32KB, use native Read with offset/limit instead, since rtk silently truncates >32KB), `rtk grep "<pattern>" <file>` for targeted search, `rtk find "<glob>" .` for file discovery, `rtk ls <dir>` for directory listing. You may use python3 -c one-liners for JSON parsing. Do NOT spawn any further sub-agents (you are depth-1, leaf). Do NOT modify any files.

CLAIMS TO VERIFY (each independently):

CLAIM C1 (Conmy seed=0 hardcode): The vendored Conmy ACDC code hardcodes the IOI-dataset seed=0 at `literature/research-for-faithful-implementation-and-extension/Conmy-Towards-Automated-Circuit-Discovery-for-Mechanistic-Interpretability/author-code/acdc/ioi/utils.py` line ~44, inside `get_all_ioi_things`, and this seed is NEVER overridden by the project's `--seed` CLI arg or `MI_SEED` env var. Verify: (a) read that file at line 44 area, confirm the hardcoded seed=0; (b) read the callers `src/phase3/run_s5b_acdc_real.py` (#15), `src/phase3/run_g8_acdc_rtn_sweep.py` (#19), `src/phase3/run_s5b_acdc_quant.py` (#16) — confirm none of them pass a seed into the Conmy IOI data loader; (c) confirm whether `MI_SEED`/`--seed` reaches the Conmy data path at all.

CLAIM C2 (#15 "fp16" is actually fp32): `src/phase3/run_s5b_acdc_real.py` loads the model via Conmy's `HookedTransformer` default which is fp32 (look for a code comment around lines 56-59 confirming this), NOT genuine torch.float16. By contrast `src/phase3/run_g8_acdc_rtn_sweep.py` (#19) uses genuine torch.float16 for its fp16 baseline. Verify: (a) read run_s5b_acdc_real.py around 56-59, find the fp32 default + comment; (b) read run_g8_acdc_rtn_sweep.py, find where it sets torch.float16; (c) confirm num_examples / tau / max_steps / dataset are otherwise matched between #15 and #19.

CLAIM C3 (#19 disk bit-identical across "seeds 0-9"): The files `data/seed_runs/phase3/phase3_g8_acdc_rtn_jaccard_seed{0..9}.json` are bit-identical in their edge_jaccard values across all 10 seeds (e.g. rtn8:1.0, rtn7:1.0, rtn6:0.923, rtn5:0.6, rtn4:0.667 for every seed). This would prove n_effective=1 (determinism confirmation, not 10 independent samples). Verify: python3 -c to load all 10 JSONs, extract the edge_jaccard dict per precision, and assert they are identical across seeds. Report the actual values.

CLAIM C4 (#15 disk bit-identical): The files for #15 (B3 ACDC real) — find them on disk (likely `data/phase3_s5b_acdc_real*.json` or under `data/seed_runs/phase3/`) — have identical `final_kl` (claimed = 2.940192699...) across all 10 seeds. Verify by loading each and comparing.

CLAIM C5 (#14 and #17 genuinely resample): `src/phase3/run_s1_ioi.py` (#14) and `src/phase3/run_s5b_acdc_iii_int8.py` (#17) use the project's OWN `generate_ioi_dataset(n, MI_SEED)` (in `src/phase3/ioi_dataset.py`) which genuinely resamples per seed, so their n_eff=10 is real. Verify: (a) read ioi_dataset.py, confirm `generate_ioi_dataset` uses the seed for RNG; (b) read run_s1_ioi.py, confirm it calls generate_ioi_dataset with MI_SEED; (c) read run_s5b_acdc_iii_int8.py, confirm same; (d) load #14 disk artifacts (`data/phase3_s1_ioi.json` + `data/seed_runs/phase3/phase3_s1_ioi_seed{1-9}.json`) and confirm the null_mean/null_std/frac_correct DIFFER across seeds (proving genuine resampling).

CLAIM C6 (#16 B4-G gate numbers): The disk file `data/phase3_s5b_acdc_quant_full.json` (or similar) shows: fp16 PASS KL=8.05e-5; int8 FAIL KL=8.47e-3 (~8.5x over tol 1e-3); int4 PASS KL=2.62e-5; edge_jaccard int4=0.923; n=4 seeds (0-3) only. Verify by loading the file and reading these exact numbers. Also confirm this is bnb (not RTN).

CLAIM C7 (#17 methodologically distinct from #14): #17 uses n=50 (vs #14 n=120), K=10 (vs K=5), z_thresh=2.0 (vs 2.5), tests 13 B3-discovered heads (vs 27 hand-circuit). Verify by reading run_s5b_acdc_iii_int8.py defaults and run_s1_ioi.py defaults.

CLAIM C8 (#19 RTN verified, not bnb): run_g8_acdc_rtn_sweep.py uses `build_rtn_tl`/`rtn_quantize_` (from src/common/rtn_quant.py) and does NOT use BitsAndBytesConfig. Verify by reading the runner imports + the quantization call site.

RETURN FORMAT: For each claim C1-C8, a verdict line:
`C{n}: CONFIRMED|REFUTED|PARTIAL|CANNOT-VERIFY — <one-line evidence with file:line or disk path>`
Then a 3-sentence summary: is the circuits-track faithfulness audit accurate? Any claim Opus got wrong? Any blind spot Opus missed?

Be rigorous. These are paper-load-bearing claims.


---

